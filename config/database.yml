default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: chrono_flow_mvp_development

test:
  <<: *default
  database: chrono_flow_mvp_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
